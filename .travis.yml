language: python

python:
  - 2.7
  - 3.5

install:
  - pip install --quiet flake8 coverage mock

script:
  - make test
  - make pages

deploy:
    provider: script
    script: make deploy
    on:
        branch: master
        python: 3.5
